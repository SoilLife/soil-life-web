import { CardSixFProps } from './card-six-f.interfaces';

export function Card6F({ color, ctaText, icon, subtext, text, className }: CardSixFProps) {
  return <div className={`${className}`}></div>;
}
